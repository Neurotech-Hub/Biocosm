/**
 * User-visible app version (footer). Bump this when shipping a major user-facing change.
 * Optionally align `package.json` "version" for releases, but the UI reads this constant.
 */
export const APP_RELEASE_VERSION = "1.3.0";
